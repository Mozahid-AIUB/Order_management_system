{
  "extends": "./tsconfig.json",
  "compilerOptions": {
    // Without this the compiler also picks up prisma/, which pushes the
    // output down to dist/src/main.js instead of dist/main.js.
    "rootDir": "./src"
  },
  "include": ["src/**/*"],
  "exclude": ["node_modules", "test", "dist", "dist-seed", "**/*spec.ts"]
}
